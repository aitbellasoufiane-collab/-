# دليل نشر تطبيق "سوق المعلم" على Cloudflare Pages

لقد قمت بإعداد جميع الملفات اللازمة. إليك الطريقة الصحيحة لتشغيل الموقع:

### الخيار الأفضل: الربط مع GitHub (أوتوماتيكي)
هذا هو الخيار الذي أنصحك به لأنه سيتكفل بكل شيء:
1. اذهب إلى **Cloudflare Pages**.
2. اضغط على **Connect to Git**.
3. اختر المستودع الخاص بك (Repository).
4. في إعدادات البناء (**Build settings**)، أدخل ما يلي:
   - **Framework preset**: `Vite`
   - **Build command**: `npm run build`
   - **Build output directory**: `dist`
5. اضغط على **Save and Deploy**.

### الخيار الثاني: الرفع اليدوي (Direct Upload)
إذا كنت تريد الرفع يدوياً (كما في الصورة التي أرسلتها)، **لا ترفع ملفات الكود المصدرية برابط مباشر**. يجب عليك رفع ملفات "البناء" (Built files):
1. قم بتحميل المشروع (Export to ZIP) من قائمة الإعدادات في AI Studio.
2. افتح المجلد على جهازك.
3. قم بتشغيل الأوامر التالية (يجب أن يكون لديك Node.js مثبت):
   ```bash
   npm install
   npm run build
   ```
4. سيظهر لك مجلد جديد اسمه `dist`.
5. **هذا المجلد `dist` هو الذي يجب أن تسحبه وتضعه في تلك الخانة التي تظهر في الصورة.**

### ملاحظة هامة حول "الباك-إند" (Backend)
تطبيقك يستخدم `server.ts` لعمليات مثل Gemini AI. Cloudflare Pages مخصص للمواقع الساكنة (Static Sites). إذا كنت تريد تشغيل الـ Backend أيضاً، ستحتاج لاستخدام **Cloudflare Workers** وتعديل الكود قليلاً، أو البقاء على منصات مثل **Cloud Run** (التي تستخدمها الآن) لأنها تدعم Node.js بشكل كامل.

لقد أضفت ملف `public/_redirects` لضمان اشتغال الروابط (Navigation) بشكل صحيح.
